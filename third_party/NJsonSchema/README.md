Place pinned NJsonSchema/Newtonsoft dependencies here for canonical validation.

Expected minimum:
- NJsonSchema.dll
- Newtonsoft.Json.dll
- dependencies required by the chosen pinned NJsonSchema package

Record exact package versions and SHA256 hashes in DEPENDENCIES.lock before production use.
